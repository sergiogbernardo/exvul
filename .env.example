# Optional NVD API key. Without it the data builder still works, but the NVD
# rate limit is lower (5 requests / 30s instead of 50). Request one at
# https://nvd.nist.gov/developers/request-an-api-key
# In CI this is provided as the NVD_API_KEY GitHub Actions secret.
NVD_API_KEY=
